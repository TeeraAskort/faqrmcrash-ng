import { WorkerFilterPipe } from './worker-filter.pipe';

describe('WorkerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
