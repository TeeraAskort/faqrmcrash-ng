import { CropFilterPipe } from './crop-filter.pipe';

describe('CropFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CropFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
